# KENTECHTIME — 프로젝트 개요

## 1. 문제 · 타겟 유저 · 서비스 설계

**문제**
KENTECH 학생들이 128학점 졸업요건을 직접 관리해야 하는데, 카테고리(EF·EL·VC·MN·HASS 등 13종)별 이수 현황을 한눈에 파악할 도구가 없음. 수강신청 경쟁률 확인도 불편.

**타겟 유저**
KENTECH 재학생 — 학점 관리 + 수강신청 준비가 필요한 모든 학년

**서비스 설계**
Google 로그인 → 온보딩(학번·트랙·ESP 레벨 설정) → 시간표 편집 → 졸업요건 대시보드 확인 → 수강 경쟁률 트래커 조회

---

## 2. 라이브 데모 (핵심)

- 시간표 그리드에서 과목 추가·삭제, 카테고리별 색상으로 시각화
- 대시보드: 카테고리별 이수/필요 학점 도넛 차트 + 진행 바
- 북마클릿으로 학교 포털에서 성적 데이터를 그대로 가져와 자동 반영
- 수강 희망 경쟁률 실시간 트래커 (10분 주기 갱신, 시간표 확정 후 활성화)

---

## 3. 시스템 아키텍처

- **프론트엔드:** Next.js 14 App Router + Google OAuth
- **백엔드/DB:** MongoDB (사용자·시간표·트래커 데이터)
- **테마 관리:** `ThemeContext` (React Context) — 10가지 컬러 테마 + 다크 모드 전역 관리
- **다크 모드:** `dark.css` + `[data-dark="true"]` 속성 셀렉터로 CSS 오버라이드
- **트래커:** `trackerAPI` 가 주기적으로 학교 수강신청 시스템 데이터 폴링

---

## 4. 핵심 코드 파일

| 파일 | 역할 |
|---|---|
| `lib/themes.js` | 10개 컬러 테마 + DARK_THEME(쿨 그레이 13단계) catColors 정의 |
| `components/Dashboard/Dashboard.jsx` | 졸업요건 계산 로직, 도넛 차트, ESP 진행 바 |
| `components/Timetable/TimetableGrid.jsx` | 시간표 그리드 렌더링, 과목 추가/삭제, ESP 단계 추론 |
| `contexts/ThemeContext.jsx` | 테마·다크 모드 상태 관리, localStorage 영속화 |

---

## 5. AI 활용 과정

- `components/Chat/Chat.jsx` — AI 채팅 기능 (수강 과목 추천, 졸업요건 Q&A)
- `lib/gradeParser.js` — 북마클릿으로 성적 데이터 자동 파싱 → 카테고리 자동 분류
- ESP 시작 레벨 설정에 따라 이전 단계 과목을 자동으로 기수강 처리

---

## 6. 한계 · 개선 방향

**현재 한계**
- KENTECH 전용 (학사편람 고정값 하드코딩)
- 수강신청 트래커는 학교 시스템 구조 변경 시 깨짐
- 모바일 UX 미완성

**개선 방향**
- 학번별 학사편람 버전 자동 선택
- 트래커 크롤링 안정화
- 시간표 충돌 감지 알림
- PWA 앱화
